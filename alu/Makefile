SIM = icarus  # Use the desired simulator (e.g., questa, xsim, verilator)
TOPLEVEL_LANG = verilog
VERILOG_SOURCES = alu.sv  # Path to your SV file
TOPLEVEL = alu  # Name of the top-level module in your SV code
MODULE = test_alu  # Name of the Python test module

include $(shell cocotb-config --makefiles)/Makefile.sim
